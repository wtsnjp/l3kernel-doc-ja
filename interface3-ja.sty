\NeedsTeXFormat{pLaTeX2e}[2018/03/09]
\ProvidesPackage{interface3-ja}[2020/02/02 Japanese interface3 style]

% ------------------------------------------------------------- 依存パッケージ

\RequirePackage{docmute}
\RequirePackage[verb]{bxghost}

% ------------------------------------------------------------- フォント設定

%\RequirePackage[defaultsups]{newtxtext}       % 脚注記号は LaTeX 標準のまま
%\RequirePackage[varqu,varl]{inconsolata}      % タイプライタ体
%\RequirePackage[bigdelims,vvarbb]{newtxmath}  % 黒板太字は STIX

% ------------------------------------------------------------- ゴースト保護

\ExplSyntaxOn
\DeclareDocumentCommand { \code } { m }
  { \eghostguarded { \texttt {#1} } }
\cs_new_eq:NN \__wtpl_enquote:n \enquote
\DeclareDocumentCommand { \enquote } { m }
  { \eghostguarded { \__wtpl_enquote:n {#1} } }
\cs_new_eq:NN \__wtpl_codedoc_cmd:nn \__codedoc_cmd:nn
\cs_set_protected:Npn \__codedoc_cmd:nn #1#2
  { \eghostguarded { \__wtpl_codedoc_cmd:nn {#1} {#2} } }
\ExplSyntaxOff

%% all done
\endinput
